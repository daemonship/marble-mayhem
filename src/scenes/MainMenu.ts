import Phaser from 'phaser';

// MainMenu scene — shows start screen, waits for player to start
// TODO (Task 4): Show DOM start screen overlay, hide on click
export class MainMenu extends Phaser.Scene {
  constructor() {
    super({ key: 'MainMenu' });
  }

  create(): void {
    // TODO: display start screen UI
  }
}
